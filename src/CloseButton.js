import React from "react";

const CloseButton = () => {
    return (
      <span
        tabIndex="0"
        role="button"
        aria-label="Close Announcement"
      ></span>
    );
};

export default CloseButton;